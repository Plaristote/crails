cmake_minimum_required(VERSION 3.0)

file(GLOB task_files
     *.cpp)

add_executable(<%= options[:task_name] %> ${task_files})

target_link_libraries(<%= options[:task_name] %> ${dependencies})

set_target_properties(<%= options[:task_name] %> PROPERTIES OUTPUT_NAME "task")
