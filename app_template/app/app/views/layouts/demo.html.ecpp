#include <crails/params.hpp>

using namespace Crails;

Params& @params;
string* @yield;
// END LINKING

<!doctype html>
<html>
  <head>
    <title>Crails template demo layout</title>
    <meta name="csrf-token" content="<%= params->get_session()["csrf_token"].Value() %>"/>
    <script type="text/javascript" src="/assets/application.js" />
  </head>
  <body>
    <%= (yield ? *yield : "Nothing to yield") %>
  </body>
</html>
