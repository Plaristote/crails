#include <crails/sql/database.hpp>
<% sql_models.each do |model| -%>
#include "<%= model[:file] %>"
<% end -%>

using namespace Crails;
using namespace std;

void sql_database_migrate()
{
  <% sql_models.each do |model| -%>
  <%= model[:classname] %>::DatabaseMigrate();
  <% end -%>
}
