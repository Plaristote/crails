#!/bin/sh -ex

machine_name="<%= project_name %>_dock"

docker build -t "$machine_name" docker/base

mkdir -p docker/runtime

docker run -it --rm \
  -v "`pwd`:/opt/webapp" \
  "$machine_name" /usr/local/bin/package.sh
